#!/bin/bash

# ONE-LETTER ★
alias b=brew # vv
alias c=clear # vv
alias e=exit # vv
alias f="rm -rf" # x
alias h=custom_localhost # v
alias k=custom_trash # v
alias m=custom_mkdir # v
alias o=custom_open # vv
alias v=custom_vscode # v
alias t=touch # vv
alias x="cd && clear" # v
alias z=custom_zip # vv

# SHORTCUT COMMAND
alias cpwd="pwd | pbcopy"
alias delds="sudo find . -name '.DS_Store' -type f -delete"
alias kter="killall iTerm2"
alias lh=localhost
alias localsh="cd /usr/local/bin"
alias upd="source update.sh"

# DIRECTORIES
alias app="cd /Applications"
alias dev="cd ~/Dev"
alias doc="cd ~/Documents"
alias dll="cd ~/Downloads"
alias dsk="cd ~/Desktop"

# SHUTDOWN
alias bye="sudo shutdown -h now"

# REBOOT
alias rbt="sudo shutdown -r now"

# GIT
alias gmc="gitmoji -c"
alias grh="git reset --hard"

# PACKAGE MANAGER
alias ny="npm i && yarn"

# YARN
alias y=yarn

alias ya="yarn add"
alias yad="yarn add --dev"
alias yga="yarn global add"

alias yr="yarn remove"
alias ygr="yarn global remove"

alias yb="yarn build"
alias ydp="yarn deploy"
alias ydv="yarn dev"
alias ye="yarn eject"
alias yst="yarn start"
alias ysr="yarn serve"
alias yt="yarn test"
alias yw="yarn watch"

# NPM
alias n=npm

alias ni="npm i"
alias nid="npm i --save-dev"
alias nig="npm i -g"

alias nu="npm uninstall"
alias nug="npm uninstall -g"

alias nrb="npm run build"
alias nrdp="npm run deploy"
alias nrdv="npm run dev"
alias nre="npm run eject"
alias nrst="npm run start"
alias nrsr="npm run serve"
alias nrt="npm run test"
alias nrw="npm run watch"

# SVN
alias sa="svn add"
alias sco="svn checkout"
alias sc="svn commit"
alias su="svn update"
alias sst="svn status"
